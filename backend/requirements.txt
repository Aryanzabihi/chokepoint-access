fastapi>=0.111
uvicorn[standard]>=0.30
sqlmodel>=0.0.21
alembic>=1.13
psycopg2-binary>=2.9
passlib[bcrypt]>=1.7
# passlib's bcrypt backend detection breaks on bcrypt>=4.1 (reads a removed
# _bcrypt.__about__ attribute, then fails its own internal self-test against
# bcrypt's now-strict 72-byte limit). Pin below that until passlib is fixed
# or replaced.
bcrypt<4.1
itsdangerous>=2.2
jinja2>=3.1
python-multipart>=0.0.9
httpx>=0.27
pydantic-settings>=2.3
email-validator>=2.1

# reused from the existing engine (src/tar_ingest.py, services.py, client_profile.py)
pandas>=2.2
numpy>=1.26

# dev / test only
pytest>=8.0
